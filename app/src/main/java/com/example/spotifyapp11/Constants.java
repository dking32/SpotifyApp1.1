package com.example.spotifyapp11;

class SearchDisplay {
    // max number of items that can be displayed for each search result category (artists, albums, tracks)
    static final int MAX_RESULTS = 10;
    // max length of search result item name
    static final int MAX_NAME_LENGTH = 20;
    // max length of search result attribute
    static final int MAX_ATTRIBUTE_LENGTH = 22;
}
